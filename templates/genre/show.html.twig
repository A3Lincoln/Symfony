{% extends 'base.html.twig' %}

{% block title %}{{ entityName }}{% endblock %}

{% block body %}
    <h1>{{ entityName }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('genre_index') }}">back to list</a>

    <a href="{{ path('genre_edit', {'id': entity.id}) }}">edit</a>

    {{ include('_delete_form.html.twig') }}
{% endblock %}
