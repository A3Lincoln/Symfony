{% extends 'base.html.twig' %}

{% block title %}New Genre{% endblock %}

{% block body %}
    <h1>Create new {{ entityName }}</h1>
    {{ include('_form.html.twig') }}

    <a href="{{ path(entityName ~ '_index') }}">back to list</a>
{% endblock %}
